{
    "name": "@tomnlittle/types",
    "version": "0.0.3",
    "author": "Thomas Northall-Little",
    "main": "build",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "lint": "tslint -p tsconfig.json -c tslint.json",
        "lint:fix": "tslint -p tsconfig.json -c tslint.json --fix"
    },
    "files": ["build"],
    "devDependencies": {
        "typescript": "^3.1.1"
    },
    "dependencies": {}
}
